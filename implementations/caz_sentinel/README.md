# CAZ Sentinel

Runtime guard for open LLMs using Concept Allocation Zones. Wraps pythia-6.9b behind an OpenAI-compatible API and blocks prompts that activate security-concept directions above calibrated thresholds.

See `../../docs/superpowers/specs/` or the plans directory for design + implementation plans.
